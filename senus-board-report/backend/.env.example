# Required only to re-run the AI extraction pipeline (app/ai_extraction.py)
# and the live AI insights endpoint (/api/insights/generate).
# The app runs and serves the dashboard perfectly well without this key,
# using the committed extraction output at app/data/extracted_financials.json.
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxx
